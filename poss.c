# POS
#include<stdio.h>
main()
{
int a;
printf("\n enter a number");
scanf("%d",&a);
if(a>0&&a<0)
{
if(a>0)
printf("\n given number is possitive");
else
printf("\n given number is negative");
}
else 
{
printf("given number is zero");
}
}
